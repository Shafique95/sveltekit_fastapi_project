# Server Configuration
server.port=8000

# Application Info
spring.application.name=ilamicbook-backend

# Logging
logging.level.com.ilamicbook.backend=INFO

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always 